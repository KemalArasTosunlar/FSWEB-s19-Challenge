#//application name
spring.application.name=twitter

# Server Configuration
server.port=3000
#
server.servlet.context-path=/workintech/twitter
# Database Configuration
# PostgreSQL'in varsay?lan (default) port numaras? 5432 dir.
#jdbc:postgresql: protokol ismi
# // -> https:// demektir.
#localhost: -> domain adi
# postgres -> veri taban? ismi
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=k131141t
spring.jpa.hibernate.ddl-auto=update
#SQL loglarini görmek için

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

spring.application.name=twitter
